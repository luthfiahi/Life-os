/**
 * Life OS Toast Notification
 * 
 * Uses Sonner library, styled to match Life OS design system.
 * Import { toast } from 'sonner' directly in components.
 * 
 * Usage:
 *   import { toast } from 'sonner'
 *   toast.success('Berhasil disimpan')
 *   toast.error('Gagal menyimpan data')
 *   toast.info('Informasi penting')
 *   toast.warning('Perhatian')
 */

// Toast is configured in layout.tsx via <Toaster /> component.
// No additional wrapper needed - use sonner directly.

export {}